// ./src/plugins/strapi-plugin-generate-schema.js

module.exports = {
    register(/* { strapi } */) {
      // This is called before the application is initialized
    },
    bootstrap(/* { strapi } */) {
      // This is called after the application is initialized
    },
  };
  